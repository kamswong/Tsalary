# 2026-08-22

## 工资计时器 (Tsalary) — 窗口标题可配置 + 设置页对齐修复

- 需求：悬浮窗显示可自定义标题；设置页提供标题输入框；修复设置页右上角 GitHub 按钮与主题开关未对齐。
- 改动：
  - `src/calc.js` 与 `electron/main.js` 的 `DEFAULT_CONFIG` 新增 `title: '实时工资'`。
  - `src/views/DisplayView.vue`：标题由硬编码改为 `displayTitle` 计算属性（取 `cfg.title`，空值回退“实时工资”）。
  - `src/views/SettingsView.vue`：顶部新增“窗口标题”输入区（v-model `form.title`，maxlength 20），`save()` payload 写入 `title`。
  - `electron/main.js`：`createDisplay` 与 `save-config` 中同步 `displayWin.setTitle(config.title || '工资计时器')`。
  - 对齐修复：移除 `.btn-github:hover` 的 `transform: translate(0,-0.25rem)`（悬停时按钮上移导致与开关错位），改为阴影高亮；`.head-right` 显式 `align-items:center` + `justify-content:flex-end`，`.theme-box` 增加 `align-self:center`。
- 验证：`npm run build:web` 通过（17 modules transformed）。
